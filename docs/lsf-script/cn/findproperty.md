# findproperty

此命令返回包含电路中具有特定属性的所有元素的单元格。

**语法** | **描述**
---|---
findproperty (property, recursive); | 返回包含电路中具有'property'属性的所有元素的单元格。如果 recursive 为 true，则返回当前范围内所有元素的平面列表（层次结构）。recursive 的默认值为 false。

**另请参阅**

[命令列表](../命令列表.md)、[findpropertyvalue](./findpropertyvalue.md)